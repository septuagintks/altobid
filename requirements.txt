# NOTE: 需在 Python 3.10 ~ 3.12 环境安装（3.14 暂无稳定 wheel）。
# torch 请按 CUDA 版本从 https://pytorch.org 对应命令安装，此处不锁定。

# 推理核心
transformers>=4.49.0
accelerate>=0.34.0
qwen-vl-utils>=0.0.8          # Qwen2.5-VL 官方图像预处理工具
pillow>=10.0.0

# 屏幕采集与图像处理
mss>=9.0.1                    # 高速屏幕截图
opencv-python>=4.9.0          # 变化检测 / ROI resize
numpy>=1.26.0

# 配置与工具
pyyaml>=6.0

# 量化：bitsandbytes NF4 4bit（默认，8GB 显存卡推荐；运行时从 fp16 权重量化）
# 注：AWQ 在 Windows + torch2.6 下内核(awq_ext) ABI 不匹配无法加载，故改用 bitsandbytes。
bitsandbytes>=0.45.0
# GPTQ 备选（Windows 需内核编译，未必成功）：
# optimum>=1.20.0
# gptqmodel>=1.0.0

# Flash Attention 2（可选，加速；Windows 需社区 wheel 或源码编译，缺失时自动回退 sdpa）
# flash-attn>=2.5.0

# 开发/测试
pytest>=8.0.0
